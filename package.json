{
  "name": "system-npmlocator",
  "version": "0.0.1",
  "description": "Nodejs style dependency resolving for SystemJS",
  "main": "npmlocator.js",
  "devDependencies": {
    "LiveScript": "^1.3.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "lsc -c npmlocator.ls"
  },
  "author": "Jami Pekkanen <jami.pekkanen@helsinki.fi>",
  "license": "Public domain",
  "dependencies": {
    "browser-builtins": "^3.3.0"
  }
}
